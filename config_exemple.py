SECRET_KEY="4a3158a5a908ff71d94a042b1102a089"
# cette clé est une chaine de 32 caractères aléatoires, elle est utilisée pour signer les cookies de session
# elle doit être gardée secrète et ne doit pas être partagée    
# cette valeur est érronée
# on peut générer une nouvelle clé en utilisant la commande suivante:python3 -c 'import os; print(os.urandom(16).hex())'
DATABASE= "APPLIZZY.db"
#ici on définit le nom de la base de données SQLite qui sera utilisée par l'application
